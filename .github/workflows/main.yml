name: Deploy-frontend-to-stage
on: 
  push: 
    branches:
      - main
    paths:
        - 'package.json'
jobs:
  deploy-to-stage: 
    runs-on: ubuntu-latest
    steps: 
      - name: hello-world
        run: echo "Hello World!"
